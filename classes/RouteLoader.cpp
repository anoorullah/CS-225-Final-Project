#include "RouteLoader.h"
#include "AirportLoader.h"

RouteLoader::RouteLoader() {

} // Constructor to set default values for all private variables
RouteLoader::RouteLoader(string flight_, double ns, double ew) {

}
void RouteLoader::addConnection(AirportLoader connect) {

} // Adds connection (edge) between airports as a route
void RouteLoader::removeConnection(AirportLoader connect) {
    
} // Removes connection (edge) between airports