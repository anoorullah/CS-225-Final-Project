#pragma once
#include <string>
#include <fstream>
#include <map>
#include <vector>

using namespace std;

class FileReader {
public:
    FileReader();
    void readAirport(string airport);
    void readRoute(string route);
private:

};